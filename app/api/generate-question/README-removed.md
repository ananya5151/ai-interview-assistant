This folder previously contained a deprecated API route `generate-question`.
It has been removed in favor of `app/api/generate-questions`.
If Vercel still shows a route, clear the build cache and redeploy.
